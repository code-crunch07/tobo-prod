# Copy to .env and fill in values before: docker compose up -d
# Used by docker-compose for backend and as build args for dashboard/client

# Backend
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/tobo?retryWrites=true&w=majority
JWT_SECRET=your-super-secret-jwt-key-min-32-chars

# Public API URL (no trailing slash; /api is appended by the app)
# Must be HTTPS in production. Used at build time for dashboard and client.
NEXT_PUBLIC_API_URL=https://api.tobodigital.com/api
